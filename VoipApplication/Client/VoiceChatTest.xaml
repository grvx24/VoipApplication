<UserControl x:Class="VoIP_Client.VoiceChatTest"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VoIP_Client"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="White">
        <StackPanel Orientation="Vertical">
            <Label HorizontalAlignment="Center" FontSize="40" Content="Adres IP">
            </Label>
            <TextBox Name="IpTextBox">
                
            </TextBox>
            <Label HorizontalAlignment="Center" FontSize="40" Content="Port">
            </Label>
            <TextBox Name="PortTextBox">

            </TextBox>
            <Button Content="Połącz" Name="ConnectButton" Click="OnConnectButton">
                
            </Button>
            <Button Margin="0,50" Content="Nasłuchiwanie" Click="OnListenButton" Name="ListenButton">

            </Button>
        </StackPanel>
    </Grid>
</UserControl>
